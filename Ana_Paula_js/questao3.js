var entrada = require('readline-sync');

var num = parseInt(entrada.question('Digite um numero? '));
var num2 = parseInt(entrada.question('Digite outro numero? '));

console.log ('A soma de dois números digitados é: ', (num+num2)); 

